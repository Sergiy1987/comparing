{
  "private": true,
  "scripts": {
    "test": "npx percy exec --testing -- mvn test"
  },
  "devDependencies": {
    "@percy/cli": "^1.16.0",
    "@percy/script": "^1.1.0"
  },
  "dependencies": {
    "postinstall": "^0.8.0"
  }
}
