{
  "private": true,
  "scripts": {
    "test": "npx percy exec --testing -- mvn test"
  },
  "devDependencies": {
    "@percy/script": "^1.1.0"
  },
  "dependencies": {
    "postinstall": "^0.8.0"
  }
}
